age = 20
age2 = 20
print(age+age2)

#Brendan's solution
# rules 1. when concatenate all have to be strings and 2. when add numbers both have to be numbers.
age = 20
age2 = 20
print ("the sum of the values is " + str(result))
# with list would be print("The sum of the vlaues is", result)
