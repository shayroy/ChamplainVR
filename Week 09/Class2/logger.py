#The timelog file will be called by the functions.
filename = 'writelog.txt'
#datetime will be used in the open and close timestamps by the logger.
#from datetime import *
im

def writelog(filename):
    """"This function logs time in timelog."""
    opentime = datetime.datetime.now()
    with open(filename, 'a') as f:
    current_date_time = datetime.datetime.now
    f.write(current_date_time)
    sleep (5)
    f.write (current_date_time)
        with close(filename, 'a') as f:
        f.write(datetime.now)


datetime.